{
  "name": "restaurant-project",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node server.js",
    "start:prod": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check admin.js && node --check admin-login.js && node --check cart.js && node --check staff.js && node --check staff-login.js && node --check scripts/hash-password.js && node --check scripts/check-db.js && node --check scripts/backup-db.js",
    "hash-password": "node scripts/hash-password.js",
    "check-db": "node scripts/check-db.js",
    "backup-db": "node scripts/backup-db.js"
  },
  "dependencies": {
    "sqlite3": "5.1.7"
  }
}
